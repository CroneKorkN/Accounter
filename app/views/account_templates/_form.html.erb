<%= form_for(@account_template) do |f| %>
  <% if @account_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_template.errors.count, "error") %> prohibited this account_template from being saved:</h2>

      <ul>
      <% @account_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :has_inital %><br>
    <%= f.check_box :has_initial %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
